<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Strive"
        tools:targetApi="31">
        <activity
            android:name=".SnoozeCountdownActivity"
            android:exported="false" />
        <activity
            android:name=".AlarmScreenActivity"
            android:exported="false" />
        <activity
            android:name=".SnakeGameActivity"
            android:exported="false" />
        <activity
            android:name=".AlarmSetupActivity"
            android:exported="false" />
        <activity
            android:name=".ExerciseAlarm"
            android:exported ="false" />
        <activity
            android:name=".AffirmationsActivity"
            android:exported="false" />
        <activity
            android:name=".QuestionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <receiver android:name=".model.AlarmReceiver"></receiver>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>